{
  "name": "silent-failure-museum",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "An interactive, sourced gallery of real AI-agent failures — each one replayable, paired with what would have caught it.",
  "license": "MIT",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "validate": "node scripts/validate-exhibits.mjs",
    "test": "node scripts/validate-exhibits.mjs"
  }
}
